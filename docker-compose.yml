services:
  web:
    image: ${IMAGE_TAG:-kiravolkova7/educational_platform:latest}

    command: >
      sh -c "gunicorn --bind 0.0.0.0:8000 --workers 4 config.wsgi:application"

    volumes:
      - static_volume:/app/staticfiles
      - media_volume:/app/media

    ports:
      - '8000:8000'
    env_file:
      - ./.env
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started

  celery-worker:
    image: ${IMAGE_TAG:-kiravolkova7/educational_platform:latest}
    command: celery -A config worker --loglevel=info
    env_file:
      - ./.env
    depends_on:
      - db
      - redis
    volumes:
      - static_volume:/app/staticfiles
      - media_volume:/app/media

  celery-beat:
    image: ${IMAGE_TAG:-kiravolkova7/educational_platform:latest}
    command: celery -A config beat --loglevel=info --scheduler django_celery_beat.schedulers:DatabaseScheduler
    env_file:
      - ./.env
    depends_on:
      - db
      - redis
    volumes:
      - static_volume:/app/staticfiles
      - media_volume:/app/media

  db:
    image: postgres:16-alpine
    expose:
      - "5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${DB_USER}" ]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:alpine
    expose:
      - "6379"
    volumes:
      - redis_data:/data

volumes:
  pg_data:
    name: pg_data
  redis_data:
    name: redis_data
  static_volume:
    name: static_volume
  media_volume:
    name: media_volume